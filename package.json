{
  "name": "standalone-homicide",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dialogs": "^1.1.20",
    "immutable": "^3.8.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-markdown": "^2.5.0",
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "cp electron.js package.json build/ && electron build",
    "pack": "cp electron.js package.json build/ && electron-builder -w --dir --projectDir build"
  },
  "devDependencies": {
    "electron": "^1.6.11",
    "electron-builder": "^19.16.2",
    "nw": "^0.23.6-1",
    "nw-builder": "^3.4.1"
  },
  "window": {
    "toolbar": true
  },
  "main": "electron.js",
  "homepage": "./"
}
